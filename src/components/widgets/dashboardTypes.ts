/** Display currency for dashboard widgets (formatting + optional toggle). */
export type DashboardCurrency = 'ARS' | 'USD'

/** Who is viewing the dashboard; copy and data scope come from your API. */
export type DashboardViewerRole = 'admin' | 'advisor'
