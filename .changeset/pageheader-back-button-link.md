---
'@crosscode/gravity-components': minor
---

PageHeader visual refinements:

- **Title** now renders as an H3 (was H4) in the brand primary color (`#003973`).
- **Subtitle** font size reduced to `12px`.
- **Back button** is a `type="link"` Button instead of a bordered icon button, for a lighter, less boxed-in look.
- **Action buttons** render at Ant Design's normal (middle) size.
- When there is no subtitle, the title stays vertically aligned with the back button and action buttons.
