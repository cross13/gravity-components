---
'@crosscode/gravity-components': minor
---

UserMenu: add `avatarBackground` and `avatarColor` props to configure the avatar's background (color or gradient) and foreground (initials/icon) color.
